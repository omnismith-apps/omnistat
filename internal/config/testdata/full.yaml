# every knob set
project_id: 01a0c47a-0000-7000-8000-000000000001
base_url: https://api.example.test/v1
schema:
  mode: verify
  host_template: server
modules:
  cpu:
    enabled: true
    template: node
    attributes:
      usage:
        slug: cpu_usage
        template: server
        name: CPU %
        description: Renamed by the operator
  disk:
    enabled: false
http:
  timeout: 5s
  retries: 1
log:
  level: debug
  format: json
